# Mail Agent Mobile - Environment Configuration
# ================================================

# AI Service URLs
# ---------------
# Windows Cloud AI (Ollama)
WINDOWS_AI_URL=http://209.126.1.13:11434/api/generate

# Ubuntu Cloud AI (Ollama)  
UBUNTU_AI_URL=http://202.137.147.5:11434/api/generate

# API Keys
# --------
# Google Gemini API Key (optional, for fallback)
GEMINI_API_KEY=your_gemini_api_key_here

# Hugging Face API Key (optional, for fallback)
HUGGINGFACE_API_KEY=your_hf_token_here

# NVIDIA API Key (optional, for fallback)
NVIDIA_API_KEY=your_nvidia_api_key_here

# Telegram Bot Token (required)
# Get from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# Multi-User Configuration
# -----------------------
# USERS_CONFIG is a JSON array where each user has:
# - person_id: Unique identifier for the user (login ID)
# - temp_password: Initial password (user must change on first login)
# - telegram_chat_id: User's Telegram chat ID (get from @userinfobot)
# - emails: Array of email accounts for this user
# - patterns: Filter patterns for this user

USERS_CONFIG='[
  {
    "person_id": "user1",
    "temp_password": "changeme123",
    "telegram_chat_id": "123456789",
    "emails": [
      {
        "email": "user1@gmail.com",
        "password": "xxxx xxxx xxxx xxxx",
        "imap_host": "imap.gmail.com",
        "imap_port": 993,
        "enabled": true
      }
    ],
    "patterns": {
      "trusted_senders": "boss@company.com,family@gmail.com",
      "spam_keywords": "buy now,limited offer,click here,winner",
      "delete_keywords": "unsubscribe,opt-out,remove from list"
    }
  },
  {
    "person_id": "user2",
    "temp_password": "changeme456",
    "telegram_chat_id": "987654321",
    "emails": [
      {
        "email": "user2@yahoo.com",
        "password": "yyyy yyyy yyyy yyyy",
        "imap_host": "imap.mail.yahoo.com",
        "imap_port": 993,
        "enabled": true
      }
    ],
    "patterns": {
      "trusted_senders": "colleague@work.com",
      "spam_keywords": "act now,free money",
      "delete_keywords": "unsubscribe"
    }
  }
]'

# IMPORTANT SECURITY NOTES:
# -------------------------
# 1. Never commit this file with real passwords!
# 2. Copy this file to .env and fill in your actual values
# 3. Keep your .env file secure and private
# 4. Users must change their temp_password on first login
# 5. Pattern values are comma-separated strings (not arrays)
# 6. All passwords are stored hashed in the database
# 7. Telegram chat IDs are per-user for private message delivery
