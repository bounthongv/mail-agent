# Scheduling
schedule:
  enabled: true
  interval_hours: 1

# AI Settings
ai:
  provider: "openrouter"
  model: "google/gemma-2-9b-it:free"
  max_tokens: 300
  temperature: 0.3

# Local AI Settings (fallback - Qwen CLI)
localai:
  enabled: true
  provider: "qwen"
  model: "qwen2.5:3b"

# Report Settings
report:
  daily_summary: true
  max_emails_per_report: 20
