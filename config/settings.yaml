# Scheduling
schedule:
  enabled: true
  interval_hours: 1

# AI Settings
ai:
  provider: "ollama"
  model: "qwen2.5:1.5b"
  max_tokens: 300
  temperature: 0.3

# Local AI Settings
localai:
  enabled: true
  provider: "ollama"
  model: "qwen2.5:1.5b"

# Report Settings
report:
  daily_summary: true
  max_emails_per_report: 20
